package SortingAlgorithm.BucketSort;

/**Bucket sort介绍：
 * 这种排序算法主要用于有着固定区间的data， TC = O(N)
 * 思路：
 * 将所需要的区间总共划分成n个bucket，每个bucket就代表着某一段固定的区间；
 * 将所有的input按照区间分类放进不同的bucket中，再进行sort
 *
 * 应用：
 * 仔细思考这种算法，和binary search，SW中的算法都有相似之处。有一些SW中，在window中的算法，最优化状态就是bucket sort
 * */
public class BucketSort小结 {
}
